{
  "buildCommand": "cd zomato-clone && npm install && npm run build",
  "outputDirectory": "zomato-clone/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
